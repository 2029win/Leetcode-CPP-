关于链表 注意事项
链表的定义。

链表节点的定义，很多同学在面试的时候都写不好。

这是因为平时在刷leetcode的时候，链表的节点都默认定义好了，直接用就行了，所以同学们都没有注意到链表的节点是如何定义的。

而在面试的时候，一旦要自己手写链表，就写的错漏百出。

这里我给出C/C++的定义链表节点方式，如下所示：

// 单链表
struct ListNode {
    int val;  // 节点上存储的元素
    ListNode *next;  // 指向下一个节点的指针
    ListNode(int x) : val(x), next(NULL) {}  // 节点的构造函数
};
或这样？
typedef struct ListNodeT {
    int val;
    struct ListNodeT next;
} ListNode;


第一题  

Q ：while(head && head->val == val) {
    temp = head;
    // 将新的头结点设置为head->next并删除原来的头结点
    head = head->next;
    free(temp);对于这一步来说，所有的头节点不都是head吗，这么一改难道不是所有的头节点都改了？

A：head 不是链表节点本身，只是一个「指向头节点的指针」—— 它是一个存地址的变量，就像一张写着 “火车头位置” 的纸条。
head = 你手里的一张纸条，上面写着当前火车头的车厢编号（内存地址）
修改head = 把纸条上的编号擦掉，改成下一节车厢的编号，不会对真实的车厢有任何影响（除非你用free拆车厢）
我们就用题目里的示例 3：输入链表是[7,7,7,7]，val=7（4 个节点全是 7，要全删掉）。
我们先给这 4 个节点起个名字，方便描述：
第 1 个 7：节点 A（内存地址 0x100）
第 2 个 7：节点 B（内存地址 0x200）
第 3 个 7：节点 C（内存地址 0x300）
第 4 个 7：节点 D（内存地址 0x400）
初始状态（代码执行前）：
head里存的是0x100（指向节点 A，纸条上写着节点 A 的地址）
节点 A 的next存的是0x200（指向节点 B）
节点 B 的next存的是0x300（指向节点 C）
节点 C 的next存的是0x400（指向节点 D）
节点 D 的next存的是NULL（链表结束）

意思是head只是首个节点的地址，注意是地址，而不是一般写代码的承载体，并不是说所有的节点的首地址都是head。

Q:为什么struct ListNode* temp;struct ListNode *cur = head;要写成这样的形式
星号 * 为什么一会儿挨着类型，一会儿挨着变量？


A：这两种写法在编译器眼里是 100% 完全等价的！
但是 一行定义多个指针时容易踩坑（见下文）
// ✅ 正确写法：a 和 b 都是指针 
struct ListNode *a, *b; 

// ❌ 错误写法：a 是指针，但 b 只是普通的结构体变量（不是指针！）
struct ListNode* a, b; 


知识点：1.


























